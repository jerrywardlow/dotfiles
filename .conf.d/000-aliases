alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias ~='cd ~'

alias du='du -h'

alias dirs='dirs -v'

alias gpl='git pull'
alias gplr='git pull --rebase'
alias gpu='git push'
alias gs='git status'

alias ll='ls -ahl'
alias l='ll'
alias ls='ls --color --almost-all'

alias vdf='vagrant destroy -f'
alias vdfu='vagrant destroy -f && vagrant up'
alias vh='vagrant halt'
alias vs='vagrant status'
alias vssh='vagrant ssh'
alias vu='vagrant up'

alias tf='terraform'

alias aplay='ansible-playbook'

alias myip="curl http://ipecho.net/plain; echo"
alias speedtest="wget -O /dev/null http://speedtest.wdc01.softlayer.com/downloads/test100.zip"

alias danger='python -m SimpleHTTPServer 8000'

alias dockbomb='docker volume rm $(docker volume ls -f dangling=true -q)'

alias douche='du -sh'

alias vbox='vboxmanage'
