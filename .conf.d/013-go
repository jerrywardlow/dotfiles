# Golang stuff

if ! [[ $PATH == *"/usr/local/go/bin"* ]] && [ -d "/usr/local/go/bin" ]; then export PATH=$PATH:/usr/local/go/bin; fi
if ! [[ $PATH == *"$GOPATH/bin"* ]] && [ -d "$GOPATH/bin" ]; then export PATH=$PATH:$GOPATH/bin; fi
